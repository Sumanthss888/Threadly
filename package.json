{
  "name": "threadly-fullstack",
  "version": "1.0.0",
  "description": "Threadly Premium E-Commerce Platform - MERN Stack",
  "scripts": {
    "setup": "npm install && npm run install:all",
    "install:all": "npm install --prefix backend && npm install --prefix frontend && npm install --prefix admin",
    "start": "concurrently \"npm run server --prefix backend\" \"npm run dev --prefix frontend\" \"npm run dev --prefix admin\"",
    "build": "npm run build --prefix frontend && npm run build --prefix admin"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
